One process sends a message to a named **queue/topic**, and the **broker** ensures the message is delivered to one or more **consumers** of or **subscribers** of the topic

A message is just sequence of bytes + metadata so we can use any encoding format.
