Loadable kernel modules are *executable code* that can be loaded into the kernel and run in kernel mode.

These modules are often used to *add additional abstractions or functionality* into the kernel to handle managing a particular hardware device.