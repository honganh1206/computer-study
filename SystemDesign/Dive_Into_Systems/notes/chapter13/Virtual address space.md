![[image-13.png]]

A process's virtual address space is divided into several sections. The top part (at the lowest addresses) can only be accessed in kernel mode.

The text section (program instructions) and the data section (global variables) are initialized from the program executable file `a.out`