A group of processes. Multiple processes in a container are in the same cgroup.

cgroups have memory/CPU limits. If over the memory limit -> Out of memory killed, otherwise slow down if using too much CPU