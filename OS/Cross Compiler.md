https://wiki.osdev.org/GCC_Cross-Compiler

A compiler that runs on platform A (host) but generates executables for platform B (target)

Why? The compiler that comes with the host system does not know it is compiling something else entirely, which will create a lot of problems in the future.

