https://wiki.osdev.org/Executable_Formats

A file format created by the compiler and linker, and it is then loaded into memory by the OS's loader.

- TEXT is the actual executable code area,
- DATA is "initialized" data,
- BSS is "un-initialized" data.